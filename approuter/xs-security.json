{
  "xsappname": "xsuaa-service-with-template",
  "tenant-mode": "dedicated",
  "scopes": [
    {
      "name": "$XSAPPNAME.Viewer",
      "description": "Minimal scope required to view the application"
    }
  ],
  "role-templates": [
    {
      "name": "BasisMonitoringDashboardAppViewerTemplate",
      "description": "Role template for users who need read-only access to the app",
      "scope-references": [
        "$XSAPPNAME.Viewer"
      ]
    }
  ],
  "oauth2-configuration": {
    "redirect-uris": [
      "http://localhost:5000/**",
      "http://localhost:8080/**",
      "https://*.hana.ondemand.com/**",
      "https://*.cfapps.us10-001.hana.ondemand.com/**"
    ]
  }
}